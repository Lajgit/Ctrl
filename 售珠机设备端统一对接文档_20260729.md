# 售珠机设备端统一对接文档

> 版本：2026-07-29（Ctrl 项目落实版）  
> 生产网关：`https://api.dzxd.top`  
> 最终 Android 包名：`com.gouzhu`  
> MQTT Broker、账号、密码和 Topic 以激活或凭证恢复接口返回值为准

## 1. 本项目确认事项

本文件结合服务端交接文档和 `Lajgit/Ctrl` 当前硬件实现，确认以下最终边界：

1. 设备只安装 `com.gouzhu`，不安装、不读取、不守护 `com.zeda.ota`。
2. 不保留旧注册激活兼容，不迁移旧批次签名、旧测试私钥、旧 MQTT 凭证或旧包名数据。
3. 每台设备由 `com.gouzhu` 在本机 Android Keystore 生成独立 ECDSA P-256 密钥对。
4. 扫码支付、套餐核销和会员取珠只有平台合法的 `dispense_marbles` 才能驱动吐珠。
5. **现金购珠例外：现金计价、欠吐保存和吐珠仍由控制板独立完成。** Android 只把控制板上报的现金事实转发服务器，服务器不得再次为同一现金事件下发现金吐珠。
6. 现金事实金额统一使用整数人民币元，例如 `1` 表示 1 元、`100` 表示 100 元。
7. 会员存珠在任务到达后先提示用户倒珠；用户点击屏幕“开始存珠”后才启动存珠电机；PD4 光眼作为实际计数。
8. 投币器支持禁止接收，设备具有真实退币机构。公共代码保留硬件适配接口，最终 GPIO 或串口协议必须按量产硬件实现，不能猜测引脚。

若本文件与服务器历史文档的“现金必须等待平台出珠”或“现金金额使用分”冲突，以本节的项目确认事项为准。

## 2. 设备职责

`com.gouzhu` 负责：

- 设备身份密钥和 MQTT 凭证安全保存；
- 后续激活、MQTT 登录、订阅、心跳、状态和故障；
- MQTT 指令校验、持久化、ACK、终态和重连补发；
- 将平台固定数量出珠转发控制板；
- 管理会员存珠页面流程和实际计数；
- 将控制板确认的收现、真实退币事实上报平台；
- App 自升级和控制板 bin 升级。

控制板负责：

- 纸钞机、投币器、投币禁止和真实退币机构；
- 现金本地计价、金额累计、欠吐、掉电恢复和吐珠；
- 吐珠/存珠电机和 PD3/PD4 光眼；
- 向 Android 上报现金金额、光眼、故障和状态。

## 3. 设备标识和身份密钥

### 3.1 deviceNo

`deviceNo` 使用规范化后的 Android 设备编号：删除空格、冒号、短横线和下划线，再转大写。正式代码不随机生成替代设备号；无法取得稳定编号时停止认证并提示生产配置错误。

### 3.2 身份密钥

- 算法：ECDSA P-256 / `secp256r1`
- 签名算法：`SHA256withECDSA`
- 签名输出：ASN.1 DER，再使用无填充 Base64URL
- 私钥：Android Keystore，不可导出
- 公钥：X.509 SubjectPublicKeyInfo，可导出标准 Base64 交付平台

平台登记只接收 `deviceNo + publicKey`，不得上传私钥。

## 4. 新版认证流程

### 4.1 请求随机数

- `nonce`：每个请求重新生成 32 位安全随机字符串；
- `timestamp`：Unix 毫秒；
- HTTP 重试必须重建请求和签名，不原样重发旧 JSON。

### 4.2 报到

```text
device-enroll|identity|{deviceNo}|{nonce}|{timestamp}
```

接口：

```http
POST /api/device/enroll
```

### 4.3 首次激活

```text
device-activation|identity|{deviceNo}|{nonce}|{timestamp}
```

接口：

```http
POST /api/device/activation
```

待认领时 10～30 秒随机退避，每次重新签名。

### 4.4 后续激活

本地已有 `mqttPassword` 时：

```text
device-activation|mqtt|{deviceNo}|{nonce}|{timestamp}
```

使用当前 `mqttPassword` 执行 HMAC-SHA256。日常启动必须先后续激活刷新 Broker、Topic、心跳和 keepAlive，再连接 MQTT。

### 4.5 MQTT 凭证恢复

平台先开启一次性恢复窗口，设备调用：

```http
POST /api/device/credential-recovery
```

签名原文：

```text
device-credential-recovery|identity|{deviceNo}|{nonce}|{timestamp}
```

设备先原子保存完整新凭证，再连接 Broker。旧凭证、旧私钥和旧包名数据不参与恢复。

## 5. MQTT Topic

设备保存并使用激活响应中的完整 `mqttTopics`，不得在业务代码中自行补齐缺失项。

必须包含：

```text
commandSubscribeTopic
reportTopics.heartbeat
reportTopics.status
reportTopics.fault
reportTopics.redemption-request
reportTopics.cash-event
reportTopics.command-result
reportTopics.upgrade-progress
```

统一 QoS 1，订阅本机：

```text
pxd/v1/device/{deviceNo}/command/#
```

## 6. 心跳、状态和故障

### 6.1 心跳

连接完成立即上报，之后按服务器间隔：

```json
{}
```

### 6.2 状态

```json
{
  "runningStatus": 0,
  "publicIp": "",
  "privateIp": "192.168.1.10",
  "networkType": "wifi",
  "privateHttpBaseUrl": "",
  "apkVersion": "1.1.0",
  "apkVersionCode": 3,
  "firmwareVersion": "1.3.0.0",
  "firmwareVersionCode": 16908288,
  "timestamp": 1785290000000
}
```

`runningStatus`：`0=IDLE`、`1=IN_GAME`、`2=FAULT`、`3=MAINTENANCE`、`4=UPGRADING`。

### 6.3 故障

首版使用服务器确认的稳定故障码，例如：

```text
DISPENSER_SENSOR_ERROR
DISPENSER_MOTOR_ERROR
COLLECTOR_SENSOR_ERROR
COLLECTOR_MOTOR_ERROR
BANKNOTE_ACCEPTOR_ERROR
COIN_ACCEPTOR_ERROR
SCANNER_ERROR
LOCAL_STORAGE_ERROR
NETWORK_ERROR
CONTROLLER_ERROR
```

日志不得输出身份私钥、MQTT 密码、完整 `operationToken` 或完整取珠码。

## 7. MQTT 指令通用信封

```json
{
  "messageId": "msg-001",
  "deviceNo": "VMIT03828355QAOC",
  "commandType": "dispense_marbles",
  "timestamp": 1785290000000,
  "data": {}
}
```

处理前必须校验本机 `deviceNo`、非空 `messageId`、支持的 `commandType`、完整字段和有效期。相同 `messageId` 不得再次执行物理动作，只能重发原 ACK 或原终态。

ACK、success、failed 都持久化原始 payload。只有收到匹配的 `command_result_ack` 且 `receiptStatus=recorded` 后，才删除待补发记录；MQTT PUBACK 不等于业务确认。

## 8. 平台固定数量出珠

支付页面、支付结果和核销响应只更新 UI，不直接发串口。

合法流程：

```text
收到 dispense_marbles
→ 校验并持久化
→ 上报 ack
→ 暂停现金接收
→ Android 通过串口 0x27 下发 quantity
→ 控制板执行
→ PD3 每颗上报
→ Android 持久化 actualQuantity
→ 上报 success/failed
→ 恢复现金接收
```

终态 `actualQuantity` 必须来自 PD3 实际计数。

## 9. 扫码核销

扫码模块读取六位取珠码后发布：

```json
{
  "requestId": "scan-20260729-0001",
  "pickupCode": "123456"
}
```

同一次重试复用 `requestId`。`redemption_response=accepted` 只显示等待，仍必须等待 `dispense_marbles`。

## 10. 会员存珠

平台下发 `collect_marbles` 后：

```text
校验并持久化任务
→ 上报 ack
→ 暂停现金接收
→ 页面提示用户先倒珠
→ 用户点击“开始存珠”
→ 串口 0x02 启动存珠电机
→ PD4 每颗上报 0x03
→ Android 每颗更新并持久化 actualQuantity
→ 用户点击“完成存珠”、达到 maximumQuantity 或发生超时
→ 串口 0x03 仅停止存珠电机
→ 上报唯一终态
→ 恢复现金接收
```

应用重启后恢复未完成任务和已确认数量，但不得自动再次启动电机；页面提示工作人员或用户确认现场后继续。这样可避免掉电恢复时重复收珠。

## 11. 现金购珠和现金事件

### 11.1 现金控制边界

现金购珠完全由控制板独立完成：

```text
现金有效接收
→ 控制板按本地单价计算数量
→ 控制板保存余额/欠吐
→ 控制板直接吐珠
→ 控制板将现金金额上报 Android
→ Android 只上报 cash-event
```

平台不得再为该现金事实下发一次现金对应吐珠；Android 也不得把 `cash_event_response` 转换为吐珠指令。

### 11.2 金额单位

控制板与 Android 的现金事实上报、Android 与服务器的 `denominationAmount` 均使用整数人民币元：

```text
1   = 1元
5   = 5元
100 = 100元
```

不使用分、小数或金额字符串。控制板内部单颗价格和不足一颗余额可继续使用“分”，二者不能混用。

### 11.3 accepted

控制板仅在纸币不可逆进入钱箱或硬币有效接收后发送串口 `CashAcceptedAmount(0x28)`：

```text
Data1=0 硬币 / 1 纸币
Data2:Data4=整数人民币元
```

Android 生成稳定 `eventNo`、保存完整 payload，再发布 `report/cash-event`。网络失败时复用相同 `eventNo` 和 payload。

### 11.4 returned

只有真实退币机构执行成功后，控制板才发送 `CashReturnedAmount(0x29)`。Android 生成 returned 事件，并尽量关联同介质、同金额最早未退的 accepted `eventNo` 为 `relatedEventNo`。

控制板的公共代码提供硬件适配函数：

```c
bool CashHardware_SetCoinEnable(bool enable);
bool CashHardware_DoReturn(uint8_t medium, uint32_t amount_yuan);
```

未实现真实硬件适配时，退币默认失败并上报 `CashReturnFailed(0x2A)`，不会误驱动未知 GPIO。

### 11.5 现金开关

`sync_cash_configuration` 在本项目只同步现金接收总开关和配置版本。Android 同时控制：

- 纸钞机：`BillEnable/BillDisable`
- 投币器：`CoinEnable/CoinDisable`

现金价格和吐珠档位仍由控制板本地配置决定，不由服务器覆盖。

## 12. 本地持久化和恢复

Android 至少持久化：

- 加密 MQTT 凭证和服务端 Topic；
- 指令原始信封；
- active dispense / active collect；
- PD3/PD4 实际计数；
- ACK 和终态 outbox；
- 现金事件 outbox；
- accepted 与 returned 的关联队列；
- 现金配置版本和接收开关。

重连顺序：

```text
后续激活
→ MQTT连接和订阅
→ heartbeat和status
→ 补发现金事件
→ 补发未确认command-result
```

恢复时不得自动重复已经开始或完成的物理动作。

## 13. 升级

只保留：

- `ota`：`com.gouzhu` 自升级；
- `ball`：控制板 bin 升级。

升级不得清除 Android Keystore 身份私钥、加密 MQTT 凭证和业务 outbox。

## 14. 联调顺序

1. 确认最终包名、签名证书和 Android Keystore 可跨同包名覆盖升级读取。
2. 生成公钥，平台登记或轮换；开启凭证恢复窗口。
3. 验证后续激活、完整 Topic、MQTT 心跳和状态。
4. 验证纸钞机和投币器启用/禁用。
5. 验证 1/5/10/20/50/100 元现金本地吐珠及 `0x28` 上报，确认 Android 不二次吐珠。
6. 验证平台 `dispense_marbles`、重复消息、PD3 实际数量和业务确认。
7. 验证用户点击开始/完成存珠、PD4 计数、App 重启恢复。
8. 实现并验证真实 inhibit 引脚和退币机构强适配，再测试 `0x29`。
9. 最后验证 App/控制板升级和断网恢复。

## 15. 当前尚未由仓库资料确认的硬件项

以下内容不能从当前原理图和代码中可靠确定，因此代码只提供安全适配接口：

- 投币器 inhibit/enable 的实际 MCU 引脚和有效电平；
- 真实退币机构的驱动引脚、时序或串口协议；
- 纸币退币与硬币退币是否使用同一机构。

量产前必须补充上述硬件定义并提供强实现，完成台架和整机验证。
